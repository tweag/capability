#! /usr/bin/env nix-shell
#! nix-shell -I nixpkgs=./nix
#! nix-shell -i bash -p cabal2nix

set -eo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)" && cd "$DIR/../.."

mkdir -p nix/haskell/capability
cd nix/haskell/capability
cabal2nix ../../../ > default.nix

# vim:ft=sh
